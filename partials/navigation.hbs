<ul class="side-nav fixed right-aligned">
    <br>
    <li>
        {{! This is a good section for a brand logo or *gasp!* an advert }}
    </li>
    {{#foreach navigation}}
        <li class="nav-{{slug}}{{#if current}} nav-current{{/if}}" role="presentation"><a href="{{url absolute="true"}}">{{label}}</a></li>
    {{/foreach}}
</ul>